module.exports = {
	siteUrl: "https://codekaizen.net",
	generateRobotsTxt: true, // Generates a robots.txt file as well
	sitemapSize: 5000, // Optional: specify max URLs per sitemap file
	changefreq: "daily", // Optional: frequency of page updates
	//priority: 0.7, // Optional: default priority for URLs
	//exclude: ["/admin/*", "/private/*"],
};
